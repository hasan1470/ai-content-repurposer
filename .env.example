# Public portfolio runs locally without accounts or paid API calls.
NEXT_PUBLIC_DEMO_MODE=true
# Optional connected AI: set demo=false, add these server values and rebuild.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
# A private code entered in Workspace settings, separate from the API key.
AI_ACCESS_CODE=
